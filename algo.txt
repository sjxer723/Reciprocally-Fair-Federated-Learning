"""
Each client i:
1. training data 
2. testing data

ALgorithm:

1. model M_0

For each epoch t:
1. central server sends model M_t to all clients
2. For each client j:
    2.0. Run M_t on her testing data and gets a *real* accuracy a_j^t and reports the central server
    2.1. Update its share s_i according to her belief of *simulated accuracy* (a_i =  1 - alpha * 1.0 / pow(sum(s_all), beta)) and cost
    
    > Updated method for estimate of the gradient of the shapley share
    2.0.' Central server select some subsets of agents and use the weights of these agents
    2.1'. The client updates M_t with her training data (with size of s_i) and sends the new weights to central server
    2.2.' The server sends these models M_t^1, ..., M_t^{l} to all agents and let them report the accuraries
    2.3.' Client j increases Delta on her share
    2.4.' The client updates M_t with her training data (with size of s_i+Delta) and sends the new weights to central server
    2.5.' The server sends these models M_t^1, ..., M_t^{l} to all agents and let them report the accurari
    2.8.' Client j can estimate the gradient of the shapley share.
    
    2.2. The client updates M_t with her training data (with size of s_i) and sends the new weights to central server
3. The central server aggregates the weights and get M_t+1
"""